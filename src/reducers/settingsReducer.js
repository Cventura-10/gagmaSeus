// the initial state of your settings component
const initialState = {}

// your reducer function
function settingsReducer(state = initialState, action) {
  switch (action.type) {
    // handle different action types here
    default:
      return state
  }
}

export default settingsReducer
