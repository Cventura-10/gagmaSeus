import React from 'react';
import styles from './Header.module.css';

function Header() {
    return (
        <header className={styles.header}>
            {/* Assume Navigation Links Here */}
        </header>
    );
}

export default Header;
