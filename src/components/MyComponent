import React from 'react';

const MyComponent = () => {
  const value = 'World';

  // Correct use of an expression, assigned to a constant
  const greeting = `Hello, ${value}`;

  return (
    <div>{greeting};</div> // Proper JSX syntax
  );
};

export default MyComponent; // Make sure to export your component
